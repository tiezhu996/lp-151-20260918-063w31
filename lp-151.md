<!-- 改编自：趣味社交主题项目提示词/lp-151/lp-151.md；后端由 NestJS + TypeORM 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目类型：全栈Web应用

## 项目主要需求

### 项目概述
构建一个匿名树洞社区平台，用户可以在不暴露真实身份的情况下倾诉吐槽、互动交流。系统为每个用户生成随机匿名昵称和身份密钥，确保隐私安全的同时维护社区秩序。

### 功能需求

1. **匿名发帖与评论**：用户无需注册即可获得随机昵称和唯一身份密钥，凭密钥管理自己发布的内容。支持发布图文帖子和匿名评论，评论也以随机昵称展示。

2. **话题标签分类**：帖子按话题标签（如#工作吐槽、#情感树洞、#生活趣事）分类浏览。管理员可维护标签库，用户发帖时可选择已有标签或创建新标签。

3. **点赞与热度排行**：支持对帖子和评论匿名点赞，按时间和点赞数综合计算热度分。首页提供热度排行和最新帖子双视图切换。

4. **每日精选推送**：系统根据点赞数和互动量自动筛选每日精选内容，在首页置顶展示。管理员可手动推荐优质内容。

5. **敏感词过滤与审核**：内置敏感词库，发帖和评论时自动检测并拦截违规内容。被拦截的内容进入审核队列，管理员可在后台审核放行或永久屏蔽。

6. **匿名身份管理**：用户可创建多个匿名身份（不同昵称和头像），在不同场景下切换使用。每个身份的发帖历史独立展示。

### 技术要求

- **英文短名（用于 Docker Compose）**：`gbtreehole`
- **前端端口**：18401
- **后端端口**：19401
- **前端**：React 18 + TypeScript + Ant Design 5 + Vite（保持不变）
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0 + Redis（缓存热门帖子和会话）
- **认证**：基于身份密钥的无密码认证（密钥存储在 localStorage），后端使用 `github.com/golang-jwt/jwt/v5` 签发和校验匿名身份令牌，`JWT_SECRET` 从环境变量读取
- **敏感词检测**：后端集成敏感词过滤中间件

### 后端目录结构（Go 标准布局）

后端代码统一放在 `backend/`，必须遵循 Go 标准布局，禁止将 handler/service/repository/model 合并到单一文件：

```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go            # 程序入口：加载配置、初始化 DB/Redis、注册路由、启动 HTTP 服务
└── internal/
    ├── config/                # 配置结构体与加载逻辑（从 .env/环境变量读取）
    ├── model/                 # GORM 模型：UserIdentity、Post、Comment、Tag、PostTag、Like、SensitiveWord、ReviewQueue 等
    ├── repository/            # 数据访问层：PostRepository、CommentRepository、TagRepository、LikeRepository、SensitiveWordRepository 等
    ├── service/               # 业务逻辑层：PostService、CommentService、IdentityService、HeatService、ReviewService 等
    ├── handler/               # Gin HTTP 处理层：AuthHandler、PostHandler、CommentHandler、TagHandler、AdminHandler 等
    ├── router/                # 路由注册：集中定义路由分组和中间件挂载
    ├── middleware/            # 中间件：IdentityAuthMiddleware（身份密钥/JWT）、SensitiveWordMiddleware、CORS、请求日志、统一错误处理等
    ├── dto/                   # 请求/响应结构体与参数校验标签
    └── constants/             # 错误码、状态枚举、热度计算公式常量、Redis key 前缀等
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


### 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：**不要写 `version:` 字段**（Compose V2 已废弃），顶层声明 `name: gbtreehole` 兜底
- 根目录 `.env` 和 `.env.example`：含 `COMPOSE_PROJECT_NAME=gbtreehole`、MySQL/Redis 配置、`JWT_SECRET`、`FRONTEND_PORT`、`BACKEND_PORT`
- `frontend/Dockerfile`：多阶段构建（Node 构建 + Nginx 托管），前端技术保持不变
- `backend/Dockerfile`：Go 多阶段构建
- `frontend/nginx.conf`：SPA 路由 + `/api/` 反向代理到 `http://backend:8080`

backend/Dockerfile 必须使用 Go 多阶段构建：
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要点：
- backend 服务内部端口统一为 `8080`，对外映射 `${BACKEND_PORT:-19401}:8080`
- MySQL 和 Redis 配置 `healthcheck`，后端 `depends_on` 等待两者就绪
- 数据库使用命名卷持久化，服务通过 Docker 网络通信
- 前端暴露 `${FRONTEND_PORT:-18401}:80`，Nginx 将 `/api/` 反向代理到 `http://backend:8080/`
- 容器名带 `${COMPOSE_PROJECT_NAME:-gbtreehole}` 前缀
- 支持 `docker compose up -d` 一条命令启动

README 要求：
- 中文说明，启动方式首选 Docker Compose，第一步提示 `cp .env.example .env`
- 本地开发启动命令：`go mod tidy`、`go run ./cmd/server`、`go build ./...`
- 包含功能列表、技术栈表格（后端写 Go 1.22 + Gin + GORM）、目录结构

## 构建验证清单（硬性约束）

- 后端必须能通过 `go mod tidy` 生成完整 `go.mod`/`go.sum`，依赖声明包括 `github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/golang-jwt/jwt/v5`、`github.com/redis/go-redis/v9` 等
- 后端必须能通过 `go build ./...`；单服务运行入口为 `go run ./cmd/server`
- 禁止将 handler、service、repository、model、middleware、dto、constants 职责合并到单一文件
- 不得使用 `version:` 字段，Compose V2 顶层声明 `name: gbtreehole`

## Git 要求

代码编写完成后，在项目根目录初始化 Git 仓库并创建 `.gitignore`：

```bash
git init
git add -A
git commit -m "init: 匿名树洞社区项目初始化"
```

`.gitignore` 必须包含以下内容：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
*.pyc
__pycache__/
```
